<h1>User details</h1>

<%= @user.full_name%>
<%= @user.email%>
<%= @user.created_at%>

<%= button_to "Edit", user_path(params[:id])%> | 
<%= button_to "Delete", user_path, method: :delete, data:{confirm:"Are you sure?"}%>
